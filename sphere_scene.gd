extends Node3D


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	print("sphere scene over here...")
	print("make the shader character")
	print("make the globe shader with ripples and bonus delauney trianglation/voronoi cell wrapped over the surface")
	
	print("todo: find the boids compute shader accidental optimisatino")

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
